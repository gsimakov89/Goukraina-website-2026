{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm install && cd admin && npm install --legacy-peer-deps && cd .. && python3 -m pip install -r requirements.txt",
  "buildCommand": "python3 build_site.py",
  "outputDirectory": "public",
  "functions": {
    "api/**/*.js": {
      "maxDuration": 10
    }
  }
}
