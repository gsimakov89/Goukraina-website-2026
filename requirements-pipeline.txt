# Optional: blog admin API + UI (local or separate host). Static build uses only Python stdlib + pipeline/.
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
python-multipart>=0.0.6
httpx>=0.27.0
# [crypto] installs `cryptography` — required for RS256/ES256 via JWKS (default Supabase signing)
PyJWT[crypto]>=2.8.0
python-dotenv>=1.0.0
